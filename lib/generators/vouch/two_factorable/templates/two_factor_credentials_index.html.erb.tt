<h1>Two-factor credentials</h1>
<%% @credentials.each do |credential| %>
  <p><%%= credential.class.name %> — <%%= credential.verified? ? "verified" : "pending" %></p>
<%% end %>
<%%= link_to "Add credential", new_<%= route_helper_prefix %>_two_factor_credential_path %>
